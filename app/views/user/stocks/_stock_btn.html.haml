- if user_signed_in?
  -if  @item.stocked?
    = link_to 'ストック解除', stock_destroy_path(@item), method: :delete, remote: true
  - else
    = link_to 'ストックする', stock_create_path(@item), method: :post, remote: true

.stock-info
  .stock-num
    ="ストック数 : #{@item.stocks.size}回"

